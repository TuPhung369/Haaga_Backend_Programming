version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd react-study
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: react-study/build
    files:
      - "**/*"
  cache:
    paths:
      - react-study/node_modules/**/*

